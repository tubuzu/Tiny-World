using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class InventoryDrop : MyMonoBehaviour
{
    // [Header("Item Upgrade")]
    // [SerializeField] protected int maxLevel = 9;
    
    // protected virtual void DropItemIndex(int index, Vector3 dropPos, Quaternion dropRot)
    // {
    //     InventoryItem itemI = this.inventory.InventoryItems[index];
        
    //     ItemDropSpawner.Instance.DropFromInventory(itemI, dropPos, dropRot);
    //     this.inventory.InventoryItems.RemoveAt(index);
    // }
}
